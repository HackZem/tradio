query FindLotById($id: String!) {
	findLotById(id: $id) {
		id
		title
		firstPrice
		currentPrice
		buyNowPrice
		views
		country
		region
		type
		expiresAt
		photos
		condition
		isActive
		isSubscribed
		_count {
			bids
		}
		user {
			avatar
			username
		}
		returnPeriod
		description
	}
}
